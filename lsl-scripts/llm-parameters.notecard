charactername:Oracle
model:llama3.2
audio_model:kokoro
audio_instructions:You are talking in a wise and calm voice.
audio_voice:af_bella
think:False

// the parameters audio_model, audio_instructions and audio_voice are not required for Generate.lsl and Conversation.lsl
// the think parameter can be used with Ollama models only, and it defaults to False.
// When enabled, Ollama may perform extra internal reasoning, which can increase response time
// and may cause timeouts for slower or larger models. Use with supported models only.


// The parameters below apply for calls to AI Talkmaster instances that use Ollama models.
// The parameters are explained here: https://github.com/ollama/ollama/blob/main/docs/modelfile.md#parameter
// Do not use quotation marks for stop parameter
// Parameters are applied by uncommenting their line, otherwise the default is used.


//num_ctx:4096
//repeat_last_n:64
//repeat_penalty:1.1
//temperature:0.7
//seed:42
//stop:AI assistant
//num_predict:42
//top_k:40
//top_p:0.9
//min_p:0.05